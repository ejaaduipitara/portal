export const environment = {
    production: false,
    title: window['env']['title'],
    host: window['env']['host'],
    authToken: window['env']['authToken'],
    superSetDomain: window['env']['superSetDomain'],
    dashboardIds: window['env']['dashboardIds'],
};